# openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout /opt/CronDataContainer/nginx/crondata.key -out /opt/CronDataContainer/nginx/crondata.crt
# openssl dhparam -out /opt/CronDataContainer/nginx/dhparam.pem 2048

server {
    listen 80 default_server;

    server_name _;
    return 301 https://$host$request_uri;
}

server {
        listen 443 ssl;
        server_name _;
   
        ssl_certificate /etc/nginx/cert/crondata.crt;
        ssl_certificate_key /etc/nginx/cert/crondata.key;
	ssl_dhparam /etc/nginx/cert/dhparam.pem;
	
	ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
	ssl_prefer_server_ciphers on;
	ssl_ciphers "EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH";
	ssl_ecdh_curve secp384r1;
	ssl_session_cache shared:SSL:10m;
	ssl_session_tickets off;
	ssl_stapling off;
	ssl_stapling_verify on;
	chunked_transfer_encoding on;
	
	resolver 127.0.0.11 valid=300s ipv6=off;
	resolver_timeout 5s;
	
        location / {
                proxy_set_header        Host $host;
                proxy_set_header        X-Real-IP $remote_addr;
                proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_set_header        X-Forwarded-Proto $scheme;
                proxy_pass              http://panel:8080;
                proxy_read_timeout      240;

	}
}


